# Session Secret Key
SESSION_SECRET=your-session-secret-key-here

# Additional AI Models Configuration (JSON format)
# Use this to add new models to existing providers or add new providers
ADDITIONAL_MODELS={}

# Example:
# ADDITIONAL_MODELS={"openai": {"models": ["gpt-4-turbo", "gpt-4-vision-preview"]}}
